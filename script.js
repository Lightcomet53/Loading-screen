// You can add functionality here to control the loader
// For example, to hide the loader when your content is ready:

function hideLoader() {
    const loader = document.querySelector('.loader-container');
    loader.style.display = 'none';
}

// Example usage:
// setTimeout(hideLoader, 3000); // Hide loader after 3 seconds 